{
  /* String.Prototype.charAt

retornar o carácter de um determinado índice.*/

  const message = "HELLO WORLD";
  const letter = message.charAt(message.length - 1);
  const result = `The last letter is : ${letter}`;
  console.log(result);
}
